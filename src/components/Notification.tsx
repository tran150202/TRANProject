import React from "react";

const Notification = () => {
  return (
    <div className="h-12 bg-blue-400 text-white px-4 flex items-center justify-center text-center text-sm md:text-base cursor-pointer whitespace-nowrap overflow-x-auto max-w-full">
      Miễn phí giao hàng cho tất cả đơn hàng trên $50. Đặt món ngay hôm nay!
    </div>
  );
};

export default Notification;
